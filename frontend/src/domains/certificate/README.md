# Certificates UI

This domain provides a certificate issuance and verification screen using a Polygon testnet smart contract and IPFS metadata (Pinata).

Required env vars:
- VITE_CERTIFICATE_CONTRACT_ADDRESS
- VITE_POLYGON_CHAIN_ID (defaults to 80002)
- VITE_PINATA_JWT
- VITE_PINATA_GATEWAY (optional)
